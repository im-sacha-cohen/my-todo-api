{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success mb-3">
        <i class="bi bi-pen"></i>
        {{ button_label|default('Enregistrer') }}
    </button>
{{ form_end(form) }}
